[project]
name = "functions"
version = "0.1.0"
description = "Counts pages on .docx files"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "awslambdaric>=2.0.12",
    "pypdf>=5.0.0",
]
